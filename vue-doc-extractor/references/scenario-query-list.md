# 查询列表页 — 提取指南

## 提取清单

输出使用通用 UI 术语。

### 1. 搜索/筛选区域

查找 `<a-form>` 包裹的搜索区域，对每个 `<a-form-item>` 提取：字段标签、字段名、控件类型(**转通用术语**)、选项来源(参考对应组件库的 patterns 文件中的下拉选项来源章节)、业务含义

同时提取：搜索按钮触发函数、重置按钮行为、展开/收起功能

### 2. 查询参数组装（关键）

追踪搜索触发函数内部(参考 patterns-core.md 5.5节)：
- 字段如何映射为 API 请求参数(直接传/转换/排除)
- 日期范围如何拆分
- 分页参数名和来源

### 3. 列表查询 API 契约（关键）

追踪主查询 API：函数名/路径/方式 → 入参结构(逐字段) → 响应结构(列表路径+分页+单条记录字段)

### 4. 数据列表/表格

查找 `<a-table>` + `columns`，每列提取：列标题、dataIndex、展示格式(纯文本/日期/状态标签/链接等)、特殊逻辑(排序/固定)

### 5. 操作列与页面交互（关键）

每个按钮追踪：文本、权限、行为类型、**传递参数**(传了record哪些字段)、参数传递方式、目标页面/组件

删除操作额外记录：确认文案、API入参、成功行为

### 6. 工具栏/批量操作

表格上方按钮(新增/批量删除/导出)，同样追踪参数传递

### 7. 分页配置

分页参数名称(pageNo/pageSize)

### 8. 其他 API 契约

逐个记录：删除/状态变更/导出/选项加载 API，含完整入参和响应

### 9. 常量与枚举（关键）

扫描所有从外部文件 import 的常量/枚举（参考 patterns-core.md 第8节），追踪到源文件读取实际值。重点关注：
- 状态/类型映射常量（用于列渲染、标签颜色等）
- 选项列表常量（用于搜索区下拉选项）
- 列定义常量（如果 columns 从外部导入）
- 配置常量（分页大小等）

---

## 输出模板

```markdown
# [页面名称] — 查询列表页

## 概述
[业务功能、管理什么数据、面向什么角色]

## 搜索筛选区

| 序号 | 字段标签 | 字段名 | 控件类型 | 选项来源 | 业务含义 |
|------|---------|--------|---------|---------|---------|
| 1 | 用户名 | username | 文本输入框 | - | 模糊搜索 |
| ... | | | | | |

- 搜索触发: `handleSearch()`
- 重置: 清空条件并重新查询
- 展开收起: [有/无]

## 查询参数组装

| 前端字段 | 接口参数 | 转换逻辑 |
|---------|---------|---------|
| username | username | 直接传递 |
| dateRange | startDate/endDate | 拆分为两个日期字符串 |
| (分页) | pageNo/pageSize | pagination对象取值 |
| ... | | |

## 数据列表

| 序号 | 列标题 | 数据字段 | 展示格式 | 备注 |
|------|-------|---------|---------|------|
| 1 | 状态 | status | 状态标签: 1=启用(绿) 0=禁用(红) | 支持排序 |
| ... | | | | |

- 行标识: `id` | 行选择: [无/多选] | 滚动: [无/横向]

## 操作列

| 序号 | 按钮 | 权限 | 可见条件 | 行为 | 传递参数 | 目标 |
|------|------|------|---------|------|---------|------|
| 1 | 查看 | - | - | 页面跳转 | `{id}` via params | `/user/detail/:id` |
| 2 | 删除 | user:delete | - | 确认→API | `record.id` | `deleteUser(id)` |
| ... | | | | | | |

### 操作详细说明
[逐个说明完整交互流程、参数传递、成功后行为]

## 工具栏

| 按钮 | 权限 | 行为 | 传递参数 |
|------|------|------|---------|
| 新增 | user:add | 跳转 `/user/add` | 无 |
| ... | | | |

## 分页
- 默认每页: 10 | 可选: [10,20,50] | 参数名: pageNo/pageSize | 总数: `data.total`

## 接口契约

### 1. 查询列表 — `getUserList`
- 来源: `@/api/user` | 方式: GET | 路径: `/api/user/list`

**请求参数:**
| 参数名 | 类型 | 必传 | 说明 |
|--------|------|------|------|
| ... | | | |

**响应数据:**
```
{ data: { list: [{ id, username, status, ... }], total } }
```

### [其他 API 按同样格式]

## 常量定义

> 以下常量从外部文件引入，迁移时需在新项目中重建。

| 常量名 | 来源文件 | 类型 | 完整定义 | 用途 |
|--------|---------|------|---------|------|
| STATUS_MAP | `./constants` | 状态映射 | `{ 1: '启用', 2: '禁用' }` | 状态列展示文本 |
| ... | | | | |

## 引用的组件与模块
| 类型 | 名称 | 来源路径 | 用途 |
|------|------|---------|------|
| ... | | | |

## 提取说明
- 分析文件: [文件路径]
- 待确认项: [待确认内容]
```
