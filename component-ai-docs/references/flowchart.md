# Component AI Docs — 流程图

## 主流程

```
┌─────────────────────────────────────────┐
│         Skill 被触发                     │
│  (用户说: 生成组件文档 / 盘活组件 等)      │
└────────────────┬────────────────────────┘
                 │
          ┌──────▼──────┐
          │ 是否是"检查  │───是───▶ 文档保鲜流程 (独立)
          │ 文档过时"?   │
          └──────┬──────┘
                 │ 否
                 ▼
┌─────────────────────────────────────────┐
│           入口：就绪检查                  │
└─────────────────────────────────────────┘
                 │
         ┌───────▼───────┐
         │ 0a. 检查清单    │
         │ .component-    │
         │ list.json 存在? │
         └───┬───────┬───┘
             │ 不存在 │ 存在
             ▼       ▼
    ┌──────────┐  ┌──────────────┐
    │ 0b. 首次  │  │ 0c. 展示进度  │
    │ 扫描      │  │ (✅⬜⏭️🚫)  │
    │ scan-     │  └──────┬───────┘
    │ components│         │
    │ .mjs      │  用户选择优先级(高/中/低)
    └────┬─────┘         │
         │               │
    脚本输出判断:          │
    ├─ "No matches" → 询问 --dir → 重扫
    ├─ ">30 组件"   → 建议缩小范围
    └─ 成功 → 自动到 0c
                        │
                        ▼
               ┌────────────────┐
               │ 0d. 检查缓存    │
               │ .cache/ 就绪?   │
               └───┬────────┬───┘
                   │ 缺失    │ 全部存在
                   ▼         │
    ┌─────────────────────┐  │
    │  第二步：准备材料     │  │
    │  (条件性——缓存缺失    │  │
    │   时才执行)          │  │
    │                     │  │
    │  从清单取 pending    │  │
    │  组件 → BATCH       │  │
    │                     │  │
    │  ┌────────────────┐ │  │
    │  │ 2a. prepare.mjs │ │  │
    │  │  一次 Bash      │ │  │
    │  │  Props + Types  │ │  │
    │  │  + Usages       │ │  │
    │  │  → .cache/      │ │  │
    │  │   <Comp>.json   │ │  │
    │  └───────┬────────┘ │  │
    └──────────┼──────────┘  │
               │             │
               ▼             │
        .cache/<Comp>.json ═╝
               │
               ▼
┌─────────────────────────────────────────┐
│      第三步：逐组件生成文档              │
│     N=0, 每批最多 5 个暂停               │
│                                          │
│  ┌──────────────────────────────────┐   │
│  │  对 BATCH 中下一个组件:           │   │
│  │                                   │   │
│  │  3a. Read .cache/<组件名>.json    │   │
│  │  (props + types + usages          │   │
│  │   + sourceHash 一次到位)          │   │
│  │                                   │   │
│  │  3b. 推导 metadata                │   │
│  │  ├─ useWhen                       │   │
│  │  ├─ dontUseWhen                   │   │
│  │  └─ prefer                        │   │
│  │                                   │   │
│  │  3c. 取 sourceHash                │   │
│  │  (从 props 记录中获取)             │   │
│  │                                   │   │
│  │  3d. 生成并写入文件               │   │
│  │  ├─ Write index.md                │   │
│  │  ├─ Write metadata.json           │   │
│  │  └─ Read 验证两个文件非空          │   │
│  │                                   │   │
│  │  验证通过 → N += 1                │   │
│  └──────────────┬───────────────────┘   │
│                 │                       │
│         ┌───────▼───────┐               │
│         │ 3e. 判断下一步 │               │
│         └───┬───────┬───┘               │
│             │       │                   │
│     N<5 且  │       │ N=5 或            │
│    有剩余   │       │ 批次完成           │
│             ▼       ▼                   │
│         继续下一个  暂停汇报             │
│         (回到3a)   ┌──────────┐         │
│                    │ 用户确认 │          │
│                    └──┬───┬───┘          │
│                       │   │              │
│                  继续  │   │ 修改意见     │
│                  N=0   │   │ → 改文件    │
│                  继续   │   │ N-=1       │
│                  3a     │   │ 询问继续    │
│                         │                │
└─────────────────────────┼────────────────┘
                          │ BATCH全完成
                          ▼
┌─────────────────────────────────────────┐
│           第四步：收尾                   │
│                                          │
│  1. 更新 .component-list.json           │
│     (status → "done")                    │
│  2. 更新 README.md 索引                  │
│  3. 输出汇总 (完成数 + [?] 待确认项)      │
└─────────────────────────────────────────┘
                          │
                          ▼
                    流程结束
              (下次触发从入口 0a 继续)
```

## 文档保鲜流程（独立）

```
用户说: "检查组件文档是否过时" / "更新组件文档"
              │
              ▼
┌──────────────────────────────────────┐
│  1. Read components/*/metadata.json  │
│     提取 sourceHash + 源文件路径      │
│                                      │
│  2. 对每个源文件计算当前 hash          │
│     node -e "crypto.createHash..."    │
│                                      │
│  3. 比对, 输出差异清单:                │
│     ✅ 一致 | ❌ 可能过时             │
│                                      │
│  4. 建议过时组件重新准备材料并生成文档  │
└──────────────────────────────────────┘
```

## 脚本职责一览

| 脚本 | 步骤 | 输入 | 输出 | 耗时 |
|------|------|------|------|------|
| `scan-components.mjs` | 0b | 项目根目录 | `.component-list.json` | ~5s |
| `prepare.mjs` | 2a | 文件路径列表 | `.cache/<组件名>.json` | ~30s |
| (AI) | 3 | `.cache/<组件名>.json` | `components/<Comp>/index.md` + `metadata.json` | 每组件 ~2轮 |

## 文件结构

```
.ai/project-components/
├── .component-list.json          # 组件清单 (入口产出)
├── .cache/                       # 组件级缓存 (第二步产出)
│   ├── Button.json               #   props + types + usages + sourceHash
│   └── Modal.json
├── components/                   # 文档 (第三步产出)
│   ├── CompA/
│   │   ├── index.md
│   │   └── metadata.json
│   └── CompB/
│       ├── index.md
│       └── metadata.json
└── README.md                     # 索引 (第四步产出)
```
